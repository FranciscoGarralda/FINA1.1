/** Roles con acceso a Inicio y a pantallas globales que usan `dashboard.view` (alineado con `AuthContext` y rutas protegidas). */
export const DASHBOARD_ROLES = ['SUPERADMIN', 'ADMIN', 'SUBADMIN', 'OPERATOR', 'COURIER'] as const;
