# Opcional. Solo si el front estático se sirve en otro origen que el API.
# Valor: URL absoluta que termina en /api (ej. https://tu-api.up.railway.app/api)
# Sin barra final después de /api. Rebuild del front si cambia.
# VITE_API_BASE=
