# CI: mismo orden que desarrollo local (backend luego front).
# Simular localmente:
#   cd backend && go vet ./... && go test ./... && go build ./...
#   cd backend && golangci-lint run ./...  (v2: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.11.0)
#   cd backend && go install golang.org/x/vuln/cmd/govulncheck@latest && "$(go env GOPATH)/bin/govulncheck" ./...
#   cd frontend && npm ci && npm run lint && npm run test && npm run build
name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: backend/go.mod
          cache-dependency-path: backend/go.sum

      - name: Go vet, test, build
        working-directory: backend
        run: go vet ./... && go test ./... && go build ./...

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          # v1.x no analiza bien con Go 1.25+; v2.x alinea binario y toolchain.
          version: v2.11
          working-directory: backend

      - name: govulncheck
        working-directory: backend
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          "$(go env GOPATH)/bin/govulncheck" ./...

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Frontend npm ci, lint, test and build
        working-directory: frontend
        run: npm ci && npm run lint && npm run test && npm run build
