.env
backend/.env
# Front: variables locales (p. ej. VITE_API_BASE hacia API en Railway); ver frontend/.env.local.example
frontend/.env.local
.agent-debug.log

# Dependencias (npm ci en CI y en cada máquina; no versionar)
node_modules/
frontend/node_modules/

# Build de Vite (se genera con npm run build / Docker del front)
frontend/dist

# Artefactos de compilación local (no versionar)
backend/api
frontend/*.tsbuildinfo
