.env
backend/.env
.agent-debug.log

# Build de Vite (se genera con npm run build / Docker del front)
frontend/dist

# Artefactos de compilación local (no versionar)
backend/api
frontend/*.tsbuildinfo
